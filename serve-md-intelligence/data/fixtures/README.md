# Fixtures (SYNTHETIC)

Generated by `python -m backend.app.tools.make_fixtures`. Values are seeded pseudo-random numbers in plausible ranges - they are **not** real statistics and exist only so the engines, API and UI can run and be tested without downloading public data. Run `make data` for real data.
